# flask-render_template

# Objectif du projet:

- Création d'un template avec le framework flask.

* le template aura comme base le fichier index.html (c'est dans ce fichier que tout ce qui sera commun au projet sera programmé ) et rendre le fichier home.html (ce que l'utilisateur verra).

# Installer un environnement virtuel
# Windows

py -m pip install virtualenv

py -m virtualenv venv

.\venv\Scripts\activate

# Linux

pip install virtualenv 

virtualenv venv

source venv/bin/activate


# Installer les modules

pip install -r requirements.txt
